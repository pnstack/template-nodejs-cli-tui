{
  "name": "@acme/cli",
  "version": "0.2.0",
  "type": "module",
  "bin": {
    "acme": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rimraf dist || rm -rf dist",
    "lint": "echo \"(cli) no lint rules configured\"",
    "format": "prettier -w \"src/**/*.{ts,tsx}\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@acme/core": "workspace:*",
    "@acme/tui": "workspace:*",
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
